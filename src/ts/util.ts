export const random = (max: number): number => Math.floor(Math.random() * max);

// Will create interface.
interface WordValue {
  word: string;
  fontSize: number;
}

export class Word {
  constructor() {}

  addWord = () => {};
}

// Will create Add Function.
